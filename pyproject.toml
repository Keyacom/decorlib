[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "decorlib"
version = "1.0.0"
requires-python = ">=3.9"

[tool.isort]
profile = "black"